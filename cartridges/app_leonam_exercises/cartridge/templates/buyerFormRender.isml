<isdecorate template="common/layout/page">
    <h1>
        <isprint value="${pdict.title}" encoding="htmlcontent" />
    </h1>
    <form action="${URLUtils.url('buyerForm-SubmitInformation')}" class="edit-profile-form" method="POST" name="${pdict.trainingForm.htmlName}">
        <div class="form-group
        <isif condition=" ${!!pdict.trainingForm.shipping.firstname.mandatory === true}">required</isif>">
            <label class="form-control-label" for="firstname">
                <isprint value="${pdict.trainingForm.shipping.firstname.label}" encoding="htmlcontent" />
            </label>
            <input type="text" class="form-control" id="firstname" <isprint
                value=${pdict.trainingForm.shipping.firstname.attributes} encoding="off" />>
            <div class="invalid-feedback" id="form-fname-error"></div>
        </div>

        <div class="form-group
        <isif condition=" ${!!pdict.trainingForm.shipping.lastname.mandatory === true}">required</isif>">
            <label class="form-control-label" for="lastname">
                <isprint value="${pdict.trainingForm.shipping.lastname.label}" encoding="htmlcontent" />
            </label>
            <input type="text" class="form-control" id="lastname" <isprint
                value=${pdict.trainingForm.shipping.lastname.attributes} encoding="off" />>
            <div class="invalid-feedback" id="form-lname-error"></div>
        </div>

        <div class="form-group">
            <label class=" form-control-label" for="email">
                <isprint value="${pdict.trainingForm.shipping.email.label}" encoding="htmlcontent" />
            </label>
            <input type="text" class="form-control" id="email" <isprint
                value=${pdict.trainingForm.shipping.email.attributes} encoding="off" />>
        </div>

        <div class="form-group
        <isif condition=" ${!!pdict.trainingForm.shipping.country.mandatory === true}">required</isif>">
            <label class="form-control-label" for="country">
                <isprint value="${pdict.trainingForm.shipping.country.label}" encoding="htmlcontent" />
            </label>
            <input type="text" class="form-control" id="country" <isprint
                value=${pdict.trainingForm.shipping.country.attributes} encoding="off" />>
        </div>

        <div class="form-group
        <isif condition=" ${!!pdict.trainingForm.shipping.shipping.mandatory === true}">required</isif>">
            <label class="form-control-label" for="shipping">
                <isprint value="${pdict.trainingForm.shipping.shipping.label}" encoding="htmlcontent" />
            </label>
            <input type="text" class="form-control" id="shipping" <isprint
                value=${pdict.trainingForm.shipping.shipping.attributes} encoding="off" />>
        </div>

        <input type="hidden" name="${pdict.csrf.tokenName}" value="${pdict.csrf.token}" />

        <button type="submit" class="btn btn-block btn-primary">
            ${Resource.msg('button.senddata.profile', 'forms', null)}
        </button>
    </form>
</isdecorate>